<h1>New book</h1>
<%= form_with model:@book do |f| %>
  <div class="new-title">Title</div>
  <%= f.text_field :title %>
  <div class="new-body">Body</div>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>
